[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-builder-backend"
version = "1.0.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "groq",
    "pydantic",
]